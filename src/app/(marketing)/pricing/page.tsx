import { Pricing } from "@/components/marketing/pricing";

export const metadata = {
  title: "Pricing — ZEROGATE",
  description: "Free, Pro and Max plans for the ZEROGATE multi-agent code-security platform."
};

export default function PricingPage() {
  return (
    <>
      <Pricing />
    </>
  );
}
